# badge-print — печать бейджей участников конференции

Простая веб-страница для печати именных этикеток (бейджей) на **мобильный термопринтер** прямо с телефона. Никаких приложений и установки — один HTML-файл, работает в браузере.

🔗 **Открыть:** https://dimakotov1c.github.io/badge-print/

---

## Что умеет

- 📋 **Загрузка списка** участников из CSV-файла или вставкой таблицы из Excel/1С.
- 🔤 **Авто-определение** разделителя (`,` / `;` / табуляция) и кодировки (**UTF-8 / Windows-1251**) — кириллица не «ломается».
- 🏷️ **Этикетка по центру:** крупно «Имя Фамилия», ниже — Компания и Должность.
- 🎲 **Номер ЛОТО слева внизу:** если в списке заданы 5 чисел, они печатаются в левом нижнем углу. Печать можно включить/выключить галочкой; если чисел нет — ничего не печатается.
- 📐 **Настраиваемый размер** этикетки (по умолчанию **100×50 мм**) и шрифтов: имя (7 мм), строки (6 мм), номера ЛОТО (3 мм).
- 👀 **Просмотр по одной** этикетке с навигацией «Назад / Вперёд» и счётчиком.
- ✏️ **Правка на месте:** Имя, Компанию и Должность можно отредактировать прямо на этикетке в предпросмотре — изменения попадают в печать, но **не меняют исходный список**. При переходе к другому бейджу правки сбрасываются. Числа ЛОТО не редактируются.
- ☑️ **Первая строка — заголовок или данные:** галочкой можно указать, печатать первую строку или пропускать её как заголовок (полезно, если список сразу начинается с данных).
- 📤 **Печать картинкой** — этикетка формируется в PNG (≈203 dpi) и отправляется через меню «Поделиться» в приложение принтера. Есть поворот на 0/90/180/270°.
- 📦 **Групповая печать партиями** — список бьётся на партии настраиваемого размера (по умолчанию 24); выбранная партия формируется сразу пачкой PNG и отправляется через «Поделиться» одним махом (если устройство не поддерживает мультиотправку — картинки сохраняются по очереди).
- 🖨️ Запасной вариант — обычная печать через **AirPrint** и сохранение картинки в Фото/Файлы.

---

## Формат данных

Колонки по порядку: **Имя, Фамилия, Компания, Должность** и, при наличии, **5 чисел ЛОТО** (Ч1…Ч5). Первая строка по умолчанию считается заголовком и не печатается — это можно отключить галочкой, если список сразу начинается с данных. Числа печатаются, только если заданы все пять.

```csv
Имя,Фамилия,Компания,Должность,Ч1,Ч2,Ч3,Ч4,Ч5
Иван,Иванов,ООО «Рога и копыта»,Менеджер по продажам,5,14,25,47,91
Анна,Петрова,ООО "Буду делать",Финансовый директор,7,19,49,55,73
Пётр,Сидоров,ИП Сидоров,Разработчик
```

Кавычки-«ёлочки» `«»` и прямые `"…"` внутри названий обрабатываются корректно.

---

## Как пользоваться

1. Откройте страницу на телефоне.
2. Нажмите **«Загрузить пример данных»** для проверки или выберите свой CSV.
3. Если список **без строки-заголовка** (сразу с данными) — снимите галочку «Первая строка — заголовок».
4. При необходимости подправьте размер этикетки/шрифта (включая **шрифт ЛОТО**, по умолчанию 3 мм) и поворот картинки.
5. При желании поправьте текст (Имя, Компанию, Должность) **прямо на этикетке** — это не меняет список и сбрасывается при переходе к другому бейджу.
6. Пролистайте до нужного участника → **«Отправить на принтер (картинкой)»** → в меню «Поделиться» выберите приложение термопринтера.
7. Нажмите **«Вперёд ›»** и распечатайте следующую.

---

## Групповая печать партиями

Когда участников много, печатать по одному долго. Блок **«Групповая печать партиями»**:

1. Задайте **размер партии** (по умолчанию 24).
2. Список автоматически разбивается на партии: «Партия 1: 1–24», «Партия 2: 25–48» … (последняя — остаток).
3. Выберите нужную партию и нажмите **«Отправить партию (картинками)»**.

Все бейджи партии формируются как отдельные PNG и отправляются **разом** через меню
«Поделиться» (учитываются размеры, поворот и опция ЛОТО). Если устройство не поддерживает
отправку нескольких файлов сразу, картинки сохраняются по очереди.

---

## Печать на iPhone

iPhone не печатает на большинство мобильных термопринтеров через AirPrint, поэтому этикетка отправляется **картинкой** в фирменное приложение принтера через стандартное меню «Поделиться». Убедитесь, что приложение принтера установлено из App Store.

Размер этикетки можно задать на странице; при выборе носителя в приложении принтера укажите тот же размер (по умолчанию 100×50 мм).

---

## Технические детали

- Один самодостаточный файл `index.html` — без сторонних библиотек и без обращений в интернет.
- Данные участников вводятся и обрабатываются **только на устройстве**, никуда не передаются.
- Картинка этикетки рисуется на `<canvas>` в разрешении термопринтера (8 точек/мм ≈ 203 dpi).
- Размещение: GitHub Pages (ветка `main`, корень репозитория).
